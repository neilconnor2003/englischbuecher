import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.englischbuecher.app',
  appName: 'npx cap add iosnpx cap add androidnpx cap open ios',
  webDir: 'build'
};

export default config;
